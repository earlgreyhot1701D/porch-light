{
  "name": "porch-light-web",
  "private": true,
  "type": "module",
  "description": "Porch Light /api/watch serverless proxy to the watcher Lambda (the static site is served from web/ per vercel.json).",
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0"
  }
}
