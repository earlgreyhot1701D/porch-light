# Agents — the genuine model-driven loops (§38). Two of three loops are agents:
# extractor (this block) and watcher (Spec 5). The hunter is a deterministic
# Lambda, not an agent (§38). The rewrite is not an agent either (rewrite/).
