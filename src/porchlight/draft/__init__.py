# Draft scaffold — structure of a public comment, never its stance (Spec 3, §4b).
# Stance fields are empty BY CONSTRUCTION. There is no send capability anywhere
# in this codebase (never.md #4, #5). The human writes the position and sends.
