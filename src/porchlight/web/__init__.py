# Web — the read-only view contract the public site consumes (Spec 3, §25).
# Contract-first: the mock reads fixtures shaped to this contract, so wiring the
# live API at Spec 6 is a path swap, not a reshape. Copy the accepted mock's
# field shape exactly (ui-contract.md); do not invent view structure here.
