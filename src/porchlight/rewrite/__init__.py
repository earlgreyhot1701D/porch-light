# Rewrite — staff language -> plain EN -> verified -> ES -> verified (Spec 3, §21a).
# NOT an agent: temperature ~0, structured output, no tools, no loop. That is why
# it lives in rewrite/ and not agents/ (model-authority.md expressed as a directory).
